comp HalfAdder {
    in bit a;
    in bit b;

    out bit sum;
    out bit carry;

    sum = a xor b;
    carry = a and b;
}

main comp Adder {
    in bit a;
    in bit b;
    out bit s[2];

    sub HalfAdder as ha;
    ha.a = a;
    ha.b = b;
    s = <ha.carry, ha.sum>;
}
